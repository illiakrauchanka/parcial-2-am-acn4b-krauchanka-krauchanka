<?xml version="1.0" encoding="utf-8"?><!--
   Backup rules (legacy full-backup for API < 31).
   Auto backup is DISABLED in AndroidManifest (allowBackup="false") to prevent
   Firebase Auth state and per-user Firestore data migrating to another device
   or being restored in a way that leaks the signed-in account. These rules are
   therefore intentionally empty. See:
   https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content />