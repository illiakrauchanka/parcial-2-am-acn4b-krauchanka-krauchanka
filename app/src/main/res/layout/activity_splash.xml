<?xml version="1.0" encoding="utf-8"?>
<!-- Fullscreen splash: video on first launch, static image afterwards.
     Root FrameLayout catches taps = skip. Black ground so letterboxing
     around the 9:16 video blends in. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/splash_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <VideoView
        android:id="@+id/splash_video"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/splash_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/splash_static"
        android:visibility="gone"
        android:importantForAccessibility="no" />
</FrameLayout>
